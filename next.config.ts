import type { NextConfig } from "next";

/** @type {import('next').NextConfig} */
const nextConfig: NextConfig = {
//   output: 'export',
//   basePath: '', // กำหนด basePath ให้เป็นชื่อโฟลเดอร์ที่คุณวางไฟล์
//   trailingSlash: true,
//   images: {
//     unoptimized: true, // ปิดการใช้งาน Next.js Image Optimization
//   },
};

export default nextConfig;
